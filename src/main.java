public class main {
    public static void main(String[] args) throws Exception {
        Menu menu = new Menu();
        menu.mostrarMenu();

        // List<Producto> productos = new ArrayList<>();
        // productos.add(crearProductoPerecedero());
        // productos.add(crearProductoNoPerecedero());
    }
}
